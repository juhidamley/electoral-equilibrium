// CoalitionChart — unified bar chart showing pre- and post-shock coalition weights.
// Opaque bars: predicted shift (mu_i). Translucent overlay: rebalanced weights (w_i).
// Feasibility banner shown when optimizer finds no solution.
// Populates on SSE event: equilibrium.
// TODO (Week 7): implement with recharts or d3

export default function CoalitionChart() {
  return null; // stub
}
