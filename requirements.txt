# HPC/script-level runtime dependencies (pip install -r requirements.txt)
# These are not always available in the conda base environment on Hopper.
vaderSentiment>=3.3.2
zstandard>=0.22
pyreadstat>=1.2
beautifulsoup4>=4.12
google-genai>=1.0
feedparser>=6.0

# SetFit bio classifier (M5 training + Pi inference)
setfit>=1.0
datasets>=2.18
scikit-learn>=1.3
sentence-transformers>=3.0
