"""apify: PLACEHOLDER stub — the real X/Twitter (Apify) collector lives elsewhere.

This top-level collectors/ module is intentionally NOT implemented (it raises
below if imported). The working code is in the package instead:
    electoral/nlp/social.py                      (ApifyCollector interface)
    electoral/nlp/collectors/apify_x_scraper.py  (the actual Apify X scraper)
Kept only so the original devplan layout resolves; use the electoral/nlp versions.
"""

from __future__ import annotations

raise NotImplementedError("apify not yet implemented. See devplan for weekly schedule.")
